export default class Karousel{
    id:number;
    title:string;
    photo:string

    constructor(id:number,title:string,photo:string){
        // eslint-disable-next-line @typescript-eslint/no-unused-expressions, no-sequences
        this.id=id,
        this.title =title,
        this.photo=photo
    }
}